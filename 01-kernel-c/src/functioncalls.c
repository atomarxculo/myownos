void caller(){
    my_func(0xdede); // Llamamos a la función y le pasamos el valor entre parentesis para que lo pinte
}

int my_func(int arg){ // Creamos una función que tome un parametro y después lo devuelva
    return arg;
}